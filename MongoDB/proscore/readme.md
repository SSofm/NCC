# proscore-frontend
```
const config = {
  PORT: 3000,
  NEXT_PUBLIC_API_URL: 'http://localhost:8000/proscore',
  VIDEO_UPLOAD_API_URL: 'https://vsapi.proscore.soccer',
};
```
# proscore-apiservices
```
const config = {
  PORT: 8000,
  MONGO_URL: "mongodb+srv://proscore:codedrill@cluster0.vkmrm.mongodb.net/proscore",
  JWT_TOKEN_SECRET: "proscore",
  DEBUG: "true",
  ALLOWED_ORIGINS: "http://localhost:8080, http://localhost:3000",
};
```
